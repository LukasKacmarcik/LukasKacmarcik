//subint.ts

function findMatchingIndexes(lookingfor:number, arr: number[]) {
    if(arr.includes(lookingfor)) {
        let newArr = arr.
    }
}

//1. change array of numbers into array of strings
//2. use filter on array of strings with condition: must contain lookingFor number
//3. on each element of newarray use indexOf   of the stringarray.
//4. outcome put into anotherarray